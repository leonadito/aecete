services:
  web:
    build: .
    env_file: .env
    ports:
      - "127.0.0.1:${WEB_PORT:-8000}:8000"
    volumes:
      - ./db_data:/app/db
      - ./media_data:/app/media
    restart: unless-stopped
