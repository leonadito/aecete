services:
  web:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/db
      - media_data:/app/media
    restart: unless-stopped

volumes:
  db_data:
  media_data:
